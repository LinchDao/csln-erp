<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lin.csln.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lin.csln.entity.MenuDO">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_type" property="menuType"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="redirect" property="redirect"/>
        <result column="permission" property="permission"/>
        <result column="title" property="title"/>
        <result column="icon" property="icon"/>
        <result column="no_cache" property="noCache"/>
        <result column="breadcrumb" property="breadcrumb"/>
        <result column="affix" property="affix"/>
        <result column="hidden" property="hidden"/>
        <result column="always_show" property="alwaysShow"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, parent_id, menu_type, name, path, component, redirect, permission, title, icon, no_cache, breadcrumb, affix, hidden, always_show, sort, status, create_time, update_time
    </sql>

    <select id="selectMenusByRoleCodes" resultType="com.lin.csln.dto.sys.menu.MenuDTO">
        SELECT DISTINCT
        m.id id ,
        m.parent_id parentId ,
        m.path path,
        m.component component,
        m.redirect redirect,
        m.name name,
        m.title title,
        m.icon icon,
        m.hidden,
        m.sort sort
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_role r ON rm.role_id = r.id
        WHERE r.role_code IN
        <foreach collection="roles" item="role" open="(" separator="," close=")">
            #{role}
        </foreach>
        AND m.status = 1
        ORDER BY m.sort ASC
    </select>

</mapper>
