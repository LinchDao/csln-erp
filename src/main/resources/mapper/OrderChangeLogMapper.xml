<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lin.csln.mapper.OrderChangeLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lin.csln.entity.OrderChangeLogDO">
        <id column="id" property="id"/>
        <result column="master_id" property="masterId"/>
        <result column="sub_id" property="subId"/>
        <result column="before_content" property="beforeContent"/>
        <result column="after_content" property="afterContent"/>
        <result column="apply_user_id" property="applyUserId"/>
        <result column="approve_user_id" property="approveUserId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, master_id, sub_id, before_content, after_content, apply_user_id, approve_user_id, status, create_time
    </sql>

</mapper>
