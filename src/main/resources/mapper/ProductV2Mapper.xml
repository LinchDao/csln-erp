<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lin.csln.mapper.ProductV2Mapper">

    <select id="pageProductV2" parameterType="com.lin.csln.dto.product.ProductV2QueryParamDTO" resultType="com.lin.csln.dto.product.ProductPageV2RespDTO">
        SELECT id id,
               product_no productNo,
               name name,
               brand brand,
               category_id categoryId,
               cost_price costPrice,
               wholesale_price wholesalePrice,
               retail_price retailPrice,
               main_image_id mainImageId,
               status status,
               create_time createTime
        FROM product_v2
        <where>
            is_delete = 0
            <if test="params.productNo != null and params.productNo != ''">
                AND product_no LIKE CONCAT('%', #{params.productNo}, '%')
            </if>
            <if test="params.name != null and params.name != ''">
                AND name LIKE CONCAT('%', #{params.name}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.categoryId != null and params.categoryId != ''">
                AND category_id = #{params.categoryId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
